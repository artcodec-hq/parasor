name: release

on:
  # Before pushing a release tag, run workflow_dispatch with dry_run=true on the
  # exact commit you plan to tag. Tags snapshot this workflow file too.
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Skip publishing"
        type: boolean
        default: true

jobs:
  release-policy:
    name: release branch policy
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Verify release tag branch
        run: |
          if [[ "$GITHUB_REF" != refs/tags/v* ]]; then
            exit 0
          fi

          VERSION="${GITHUB_REF#refs/tags/v}"
          TAG_COMMIT="$(git rev-list -n 1 "$GITHUB_REF")"
          if [[ "$VERSION" == *-* ]]; then
            REQUIRED_BRANCH="origin/dev"
            REQUIRED_REF="refs/heads/dev"
            RELEASE_KIND="prerelease"
          else
            REQUIRED_BRANCH="origin/main"
            REQUIRED_REF="refs/heads/main"
            RELEASE_KIND="stable release"
          fi
          git fetch origin "+$REQUIRED_REF:refs/remotes/$REQUIRED_BRANCH"

          if ! git merge-base --is-ancestor "$TAG_COMMIT" "$REQUIRED_BRANCH"; then
            echo "::error::$RELEASE_KIND tag $GITHUB_REF must point to a commit contained in $REQUIRED_BRANCH"
            exit 1
          fi

  build:
    name: build & test (${{ matrix.os }})
    needs: release-policy
    strategy:
      fail-fast: false
      matrix:
        os: [macos-15, macos-15-intel, ubuntu-22.04]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: latest
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm test

  package:
    name: assemble dist tarball
    needs: build
    runs-on: ubuntu-22.04
    outputs:
      tarball: ${{ steps.pack.outputs.tarball }}
      version: ${{ steps.version.outputs.version }}
      npm_tag: ${{ steps.version.outputs.npm_tag }}
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: latest
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            VERSION="0.0.0-dev.$(date +%Y%m%d%H%M%S)"
          fi
          if [[ "$VERSION" == *-* ]]; then
            NPM_TAG="${VERSION#*-}"
            NPM_TAG="${NPM_TAG%%.*}"
          else
            NPM_TAG="latest"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "npm_tag=$NPM_TAG" >> "$GITHUB_OUTPUT"
          node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));p.version='$VERSION';fs.writeFileSync('package.json', JSON.stringify(p,null,2));"
      - run: pnpm build
      - run: node scripts/package.mjs
      - id: pack
        run: |
          cd dist
          TARBALL=$(npm pack --silent)
          echo "tarball=$TARBALL" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v7
        with:
          name: tarball
          path: dist/*.tgz
          if-no-files-found: error

  verify-publish:
    name: verify npm publish dry-run
    needs: package
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: tarball
          path: dist
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: |
          TARBALL="./dist/${{ needs.package.outputs.tarball }}"
          test -f "$TARBALL"
          npm publish "$TARBALL" --access public --dry-run --provenance --tag "${{ needs.package.outputs.npm_tag }}"

  publish-npm:
    name: publish npm
    needs: [package, verify-publish]
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      id-token: write
    if: startsWith(github.ref, 'refs/tags/v') && (github.event_name == 'push' || !inputs.dry_run)
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: tarball
          path: dist
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: npm publish "./dist/${{ needs.package.outputs.tarball }}" --access public --provenance --tag "${{ needs.package.outputs.npm_tag }}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  github-release:
    name: github release
    needs: [publish-npm]
    if: startsWith(github.ref, 'refs/tags/v') && !inputs.dry_run
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v8
        with:
          name: tarball
          path: dist
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*.tgz
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-') }}
